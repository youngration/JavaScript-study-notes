## 原始表达式
1.包括？
- 常量、字面量、关键字、变量
## 对象和数组的初始化表达式
## 函数定义表达式
## 属性访问表达式
1. 两种语法？以及机制？
- .和[]，先对左侧的表达式求值，若null或undefined，则报错，若不是对象，则转化为对象；若是.，则根据右侧的标识符查找对象，若是[]，则对[]内部的表达式求值，并转化为字符串，然后查找对象。
## 调用表达式
1. 求值顺序
- 先对函数表达式求值，再对参数表达式求值，若函数表达式的值不是可调用的，则报错，否则依次将参数赋值给形参，然后执行函数体，最后返回一个值，默认是undefined。
## 对象创建表达式
1. 求值过程
- 先创建一个空对象，然后将对象的__proto__指向函数的原型，接着将函数的this指向对象，并执行函数，最后返回this。
## 运算符概述
1.运算符的种类
- 标点符号或者关键字
### 操作数的个数
### 操作数类型和结果类型
### 左值
1. 是指?函数的返回值
- 表达式只能出现在=运算符左侧，如变量、对象的属性。函数的返回值只能是右值。
### 运算符的副作用
1. 副作用是指？有副作用的运算符？
- 包含了显式或隐式的赋值，从而改变了操作数。++、--、=、delete
### 运算符的优先级
1. 总结
### 运算符的结合性
1. 作用是？
- 一个表达式有多个同级的运算符时，结合性确定了解释器的求值顺序。
### 运算顺序
1. 复杂表达式的求值的两类运算
- 先从左到右对每个子表达式求值，再根据运算符的优先级、结合性对复杂表达式求值。条件运算符除外。
## 算术表达式
1. -、*、/、%运算过程
- 先将非数字操作数转换为数字，再进行相应的计算。若操作数转换的结果是NaN，则运算结果也是NaN。
2. 0/0结果
- NaN
3. 负数和浮点数的求模
- 负数求模先对正数部分求模再取反，浮点数求模和普通求模相同。
### +运算符
1. 画出流程图
### 一元算术运算符
1. 结合性
- 右结合
2. 对操作数的转换
- 转换为数字或NaN
### 位运算符
1. 包括？如何运算的？
- &：都为1时结果为1，否则为0
- |：都为0时结果为0，否则为1
- ~：1为0，0为1
- ^：不相同时结果为1，否则为0
- <<：向左移动右操作数指定的位数，并给低位补0
- >>：向右移动右操作数指定的位数，并给高位补符号位的数字
- >>>：向右移动操作数指定的位数，并给高位补0
2. 移位运算符的右操作数
- 0-31之间，即第5位之后的二进制位全部被忽略。
## 关系表达式
1. 返回值
- 布尔值
### 相等和不等运算符
1. 相等和严格相等的包含关系，以及流程图
- 相等包含严格相等
### 比较运算符
1. 对象的比较
- 先将对象转换为原始值，若运算符两边都是字符串，则比较之，否则全部转换为数字再比较。
2. +运算符和比较运算符的偏好
- +运算符优先进行字符串连接，比较运算符优先将操作数转换为数字。
### in运算符
1. 转换
- 将左侧的表达式的值转换为字符串，右侧的表达式的值转换为对象。
### instanceof运算符
1. 也会转换操作数
- 不会转换。若左操作数不是对象，则返回false，若右操作数不是函数，则报错。
## 逻辑表达式
### 逻辑与
1. 返回值
- 返回第一个假值或者最后一个表达式的值
### 逻辑或
1. 返回值
- 返回第一个真值或者最后一个表达式的值
### 逻辑非
1. 转换与返回值
- 先将操作数转化为布尔值，求反后再返回。
## 赋值表达式
1. 带操作的赋值运算的一个趣处。
- 若子表达式有副作用，则和普通的等价赋值表达式不同
## 表达式计算
### eval()
### 全局eval()
1. 直接eval()和别名eval()
- 直接eval()的上下文是所在函数的作用域，别名eval()的上下文总是全局作用域
### 严格eval()
## 其他运算符
### 条件运算符
### typeof运算符
1. null不是对象，但返回'object'；函数是对象，但返回'function'
### delete运算符
1. delete数组元素的问题
- 数组的length不会改变
2. 操作数与返回值
- 操作数是变量、函数、参数时，delete表达式返回false，其他情况都会返回true，即使操作失败
### void运算符
### 逗号运算符
1. 返回值
- 返回右侧表达式的值